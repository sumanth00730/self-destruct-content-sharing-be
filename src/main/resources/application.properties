spring.application.name=self-destruct-content-sharing

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://junction.proxy.rlwy.net:40461/railway
spring.datasource.username=postgres
spring.datasource.password=pOLUTPGrnkRGLCEHzIZWboFvaJjgguRi
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate / JPA Properties
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Redis Configuration
spring.data.redis.host=autorack.proxy.rlwy.net
spring.data.redis.port=42594
spring.data.redis.password=fHNGhPwEeLssfGzugmsoYbLxhSeTDtjA
spring.data.redis.username=default

# Aws Configuration
aws.accessKeyId=${access_key_id}
aws.secretAccessKey=${secret_key}
aws.s3.bucketName=${bucket_name}
aws.region=${region}

# actuator
management.endpoints.web.exposure.include=*

server.port=8081